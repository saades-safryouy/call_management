package com.pfe.callmanagement;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot application entry point for Call Management System.
 * This is the application bootstrapping class that initializes the Spring context.
 */
@SpringBootApplication
public class CallManagementApplication {

	public static void main(String[] args) {
		SpringApplication.run(CallManagementApplication.class, args);
	}

}
